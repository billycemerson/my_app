<h1>Billy Blog</h1>
<p>Selamat Datang di Billy Blog</p>

<ul>
    <% for article in @articles do %>
    <li>
        <strong><%= link_to article.title, read_path(article_id: article.id) %></strong>
        <small>
        Oleh <%= article.user.full_name %>
        Pada <%= article.created_at.to_s(:short) %>
        </small>
    </li>
    <% end %>
</ul>